import pointerEvent from "./pointerEvent.js";
// import panel from "./panel.svelte";

const camera = {
	uuid: "camera",
	name: "camera",
	group: "general",
	pointerEvent,
	// panel,
};

export default camera;
