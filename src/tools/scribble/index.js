import pointerEvent from "./pointerEvent.js";
import panel from "./panel.svelte";

const scribble = {
	uuid: "scribble",
	name: "scribble",
	group: "many lines",
	pointerEvent,
	panel,
};

export default scribble;
