// import pointerEvent from "./pointerEvent.js";
import panel from "./panel.svelte";

const symmetry = {
	uuid: "symmetry",
	name: "symmetry",
	group: "transform",
	// pointerEvent,
	panel,
};

export default symmetry;
