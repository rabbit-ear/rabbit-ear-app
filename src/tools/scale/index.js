import pointerEvent from "./pointerEvent.js";
import panel from "./panel.svelte";

const scale = {
	uuid: "scale",
	name: "scale",
	group: "transform",
	pointerEvent,
	panel,
};

export default scale;
